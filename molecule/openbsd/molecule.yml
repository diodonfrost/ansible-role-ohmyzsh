---
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: ohmyzsh-openbsd
    box: pygolo/openbsd7
    memory: 4096
    cpus: 2
provisioner:
  name: ansible
  inventory:
    host_vars:
      ohmyzsh-openbsd:
        ansible_python_interpreter: /usr/local/bin/python3
